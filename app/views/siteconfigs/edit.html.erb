<div id="editconfig">  
  <%= form_for(@siteconfigs, :url => { :action => "update", :controller => "siteconfigs" }) do |f| %>
     <%= render 'fields',:f => f %>
    <div class="actions">
      <%= f.submit "Save config" %>
    </div>
  <% end %>
</div>